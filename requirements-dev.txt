# Editable-install the promptchain package with every extra the tests need:
#   app       -> streamlit (tests/test_app_smoke.py drives the app via AppTest)
#   anthropic -> anthropic SDK (tests/test_streaming.py builds real SDK errors)
#   dev       -> pytest + httpx
#   mcp       -> the MCP SDK (tests/test_mcp_server.py drives the stdio server)
# Also pulls the package's own deps (requests, platformdirs, tomli on <3.11).
-e .[app,anthropic,dev,mcp]
